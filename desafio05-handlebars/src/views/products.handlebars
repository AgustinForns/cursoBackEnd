<div>
    <h2>Prouductos</h2>
     {{#each productos}}
        <p>Id: {{this.id}} </p>
        <p>Nombre: {{this.title}} </p>
        <p>Precio: {{this.price}} </p><br>
        <img src="{{this.url}}" alt="">
    {{/each}}
</div>